---
phase: 03-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lineaf/scheduler.py
  - src/lineaf/main.py
  - tests/test_scheduler.py
autonomous: true
requirements: [SCHD-01, SCHD-02]

must_haves:
  truths:
    - "APScheduler BackgroundScheduler starts with FastAPI lifespan and registers weekly cron job"
    - "POST /api/scrape triggers scrapers in a background thread and returns immediately"
    - "Cron job is configured for Monday 03:00 Moscow time"
  artifacts:
    - path: "src/lineaf/scheduler.py"
      provides: "APScheduler setup with start/stop functions"
      exports: ["scheduler", "start_scheduler", "stop_scheduler"]
    - path: "tests/test_scheduler.py"
      provides: "Tests for scheduler job registration and cron trigger"
      min_lines: 20
  key_links:
    - from: "src/lineaf/main.py"
      to: "src/lineaf/scheduler.py"
      via: "lifespan context manager"
      pattern: "start_scheduler|stop_scheduler"
    - from: "src/lineaf/scheduler.py"
      to: "src/lineaf/run_scrapers.py"
      via: "job target function"
      pattern: "run_scrapers.*main"
---

<objective>
Set up APScheduler for weekly automatic scraping and add a manual scrape trigger endpoint. The scheduler runs embedded in the FastAPI process via lifespan.

Purpose: Fulfills the automation requirement -- the system collects data weekly without manual intervention, and the team can trigger ad-hoc scrapes from the dashboard.
Output: scheduler.py, POST /api/scrape endpoint, scheduler tests.
</objective>

<execution_context>
@/Users/ilyamukha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ilyamukha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/lineaf/run_scrapers.py:
```python
def main(sites: list[str] | None = None) -> None:
    """Run scrapers for specified sites sequentially."""
```

From src/lineaf/main.py (current stub):
```python
from fastapi import FastAPI
app = FastAPI(title="Lineaf Parser")

@app.get("/health")
async def health():
    return {"status": "ok"}
```

From src/lineaf/api/runs.py (created by Plan 01, may not exist yet):
```python
router = APIRouter()
# GET /runs, GET /runs/freshness
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scheduler module and POST /api/scrape endpoint</name>
  <files>src/lineaf/scheduler.py, tests/test_scheduler.py</files>
  <behavior>
    - test_cron_trigger: scheduler has a job with id="weekly_scrape" and CronTrigger(day_of_week="mon", hour=3, minute=0, timezone="Europe/Moscow")
    - test_scheduler_start_stop: start_scheduler() starts the scheduler, stop_scheduler() shuts it down without error
  </behavior>
  <action>
    1. Create `src/lineaf/scheduler.py`:
       - Import BackgroundScheduler, CronTrigger from apscheduler
       - Import `main as run_scrapers_main` from `lineaf.run_scrapers`
       - Create module-level `scheduler = BackgroundScheduler()`
       - `start_scheduler()`: add job with `run_scrapers_main` as target, CronTrigger(day_of_week="mon", hour=3, minute=0, timezone="Europe/Moscow"), id="weekly_scrape", replace_existing=True. Then `scheduler.start()`.
       - `stop_scheduler()`: `scheduler.shutdown(wait=False)`

    2. Create `tests/test_scheduler.py`:
       - Test that after calling start_scheduler(), `scheduler.get_job("weekly_scrape")` exists
       - Verify the trigger is CronTrigger with correct fields (day_of_week=0 for Monday, hour=3, minute=0)
       - Call stop_scheduler() in teardown
       - Test that start + stop cycle completes without error

    CRITICAL: Use BackgroundScheduler (NOT AsyncIOScheduler) -- the scrapers call asyncio.run() internally which conflicts with an already-running event loop.
    CRITICAL: Only call scheduler.start() inside lifespan, never at module import level (prevents duplicate starts on uvicorn reload).
  </action>
  <verify>
    <automated>cd /Users/ilyamukha/Desktop/Рабочая/Lineaf/Parser && uv run pytest tests/test_scheduler.py -x -q</automated>
  </verify>
  <done>Scheduler tests pass. Job is registered with correct Monday 03:00 Moscow time cron trigger. Start/stop lifecycle works cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire scheduler lifespan and POST /api/scrape into main.py</name>
  <files>src/lineaf/main.py, src/lineaf/api/runs.py</files>
  <action>
    1. Update `src/lineaf/main.py`:
       - Add `asynccontextmanager` lifespan that calls `start_scheduler()` on startup and `stop_scheduler()` on shutdown
       - Pass `lifespan=lifespan` to FastAPI constructor
       - Include all API routers (prices, products, runs) with prefix="/api"
       - Keep /health endpoint

    2. Add `POST /api/scrape` to `src/lineaf/api/runs.py`:
       - Import threading and run_scrapers_main
       - On POST, start a daemon thread: `threading.Thread(daemon=True, target=run_scrapers_main).start()`
       - Return {"status": "started"} immediately (do NOT use FastAPI BackgroundTasks -- it blocks the worker thread pool for 10+ min scraper runs)

    NOTE: Plan 01 also modifies main.py (include_router calls) and runs.py (GET endpoints). Since both plans are Wave 1 parallel, this task MUST handle the case where Plan 01 has or has not run yet. The safest approach: this task writes the COMPLETE main.py (with lifespan + all include_router calls) and adds POST /scrape to runs.py (creating runs.py if it does not exist, or appending if it does).
  </action>
  <verify>
    <automated>cd /Users/ilyamukha/Desktop/Рабочая/Lineaf/Parser && uv run python -c "from lineaf.main import app; routes = [r.path for r in app.routes]; assert '/health' in routes; print('main.py OK')" && uv run pytest tests/test_scheduler.py -x -q</automated>
  </verify>
  <done>FastAPI app starts with lifespan (scheduler auto-starts/stops). POST /api/scrape endpoint exists. All routers included. Scheduler tests still pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_scheduler.py -x -q` — scheduler tests pass
- `uv run python -c "from lineaf.main import app"` — app imports without error
- `uv run pytest tests/ -q` — full suite green
</verification>

<success_criteria>
- APScheduler BackgroundScheduler registered with Monday 03:00 Moscow time cron job
- FastAPI lifespan starts/stops scheduler cleanly
- POST /api/scrape starts scrapers in daemon thread and returns immediately
- No asyncio.run() conflicts (BackgroundScheduler, not AsyncIOScheduler)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard/03-02-SUMMARY.md`
</output>
