---
phase: 02-scrapers
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/lineaf/scrapers/ormatek.py
  - tests/test_scrapers_ormatek.py
  - src/lineaf/run_scrapers.py
autonomous: false
requirements: [SCRP-02, SCRP-04, SCRP-05]
must_haves:
  truths:
    - "Ormatek spider loads the catalog page through Camoufox (bypasses 403)"
    - "Ormatek spider collects product URLs from all catalog pages"
    - "Each Ormatek product has name and prices extracted"
    - "Product detail pages provide available attribute fields"
    - "Running the spider inserts products + price_snapshots into the database via the pipeline"
  artifacts:
    - path: "src/lineaf/scrapers/ormatek.py"
      provides: "OrmatemScraper subclass with discovery-based selectors"
      min_lines: 80
    - path: "tests/test_scrapers_ormatek.py"
      provides: "Unit tests for Ormatek extraction logic"
      min_lines: 15
  key_links:
    - from: "src/lineaf/scrapers/ormatek.py"
      to: "src/lineaf/scrapers/base.py"
      via: "class OrmatemScraper(BaseScraper)"
      pattern: "class Ormatek.*Scraper\\(BaseScraper\\)"
    - from: "src/lineaf/scrapers/ormatek.py"
      to: "src/lineaf/scrapers/utils.py"
      via: "parse_price"
      pattern: "parse_price"
---

<objective>
Ormatek spider: discover site structure via Camoufox, then scrape all 160x200 mattresses from ormatek.com.

Purpose: Ormatek returns 403 to all non-stealth clients. The CSS selectors and pagination pattern are unknown — discovery must happen first via a live Camoufox session. This is Wave 3 because it requires: (a) Camoufox installed from Plan 01, (b) proven BaseScraper pattern from Plan 02's Askona implementation.

Output: `src/lineaf/scrapers/ormatek.py` with OrmatemScraper class; discovery results documented in code comments; unit tests.
</objective>

<execution_context>
@/Users/ilyamukha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ilyamukha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-scrapers/02-CONTEXT.md
@.planning/phases/02-scrapers/02-RESEARCH.md
@.planning/phases/02-scrapers/02-01-SUMMARY.md
@.planning/phases/02-scrapers/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 — same base class and utilities -->

From src/lineaf/scrapers/base.py:
```python
class BaseScraper:
    def __init__(self, site_name: str, catalog_url: str): ...
    async def run(self): ...
    async def goto_with_retry(self, page, url, max_attempts=3): ...
    async def delay(self): ...
    # Abstract:
    async def collect_product_urls(self, page) -> list[str]: ...
    async def extract_product(self, page, url: str) -> dict: ...
```

From src/lineaf/scrapers/utils.py:
```python
def parse_price(text: str | None) -> Decimal | None: ...
def validate_product(data: dict) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ormatek site discovery via Camoufox</name>
  <files>src/lineaf/scrapers/ormatek.py</files>
  <action>
    **Phase A — Discovery script (run interactively, results used for Phase B):**

    Write and execute a temporary discovery script that:
    1. Opens Camoufox headless browser.
    2. Navigates to `https://www.ormatek.com/catalog/matrasy/160x200/`.
    3. If page loads successfully (not 403):
       a. Dump `page.content()` first 2000 chars to see page structure.
       b. Try `page.evaluate("JSON.stringify(window.__NUXT__)")` to check for Nuxt.js SSR data.
       c. List all CSS classes of elements that look like product cards: `page.evaluate("Array.from(document.querySelectorAll('[class*=product], [class*=catalog], [class*=card], [class*=item]')).map(e => e.className).filter((v,i,a) => a.indexOf(v) === i).slice(0, 30)")`.
       d. Find product links: `page.evaluate("Array.from(document.querySelectorAll('a[href*=matras], a[href*=catalog]')).map(a => ({href: a.href, text: a.textContent.trim().slice(0,50), classes: a.className})).slice(0, 10)")`.
       e. Check pagination: look for `?page=`, `PAGEN_1=`, or pagination nav elements.
       f. Navigate to the first product detail page and repeat structure inspection for characteristics table.
    4. If page returns 403 even with Camoufox: log this as a known issue. Try with `headless="virtual"` mode (xvfb-based). If still 403, create the spider with best-guess selectors and mark as needing manual debugging.

    **Phase B — Implement OrmatemScraper based on discovery:**

    Create `src/lineaf/scrapers/ormatek.py` with class `OrmatemScraper(BaseScraper)`:

    **Constructor:**
    - `super().__init__(site_name="ormatek", catalog_url="https://www.ormatek.com/catalog/matrasy/160x200/")`

    **`collect_product_urls(self, page) -> list[str]`:**
    - Use pagination pattern discovered in Phase A (likely `?page=N` for Nuxt.js).
    - Use product card CSS selectors discovered in Phase A.
    - If `window.__NUXT__` contains product data, extract URLs from JSON (similar to Askona pattern).
    - Otherwise use CSS selectors on rendered DOM.
    - Stop when page has 0 product cards.
    - Call `self.delay()` between pages.

    **`extract_product(self, page, url: str) -> dict`:**
    - Navigate with `self.goto_with_retry(page, url)`.
    - Extract name from h1 or page title.
    - Extract prices from discovered selectors or __NUXT__ data.
    - Extract characteristics from table/list elements on detail page.
    - Field mapping: use Russian field names (same pattern as Askona/Sonum):
      - `firmness` ← "Жесткость" or similar
      - `height_cm` ← "Высота" or "Высота матраса"
      - `filler` ← "Наполнитель"
      - `cover_material` ← "Материал чехла"
      - `weight_kg` ← "Вес" or "Вес на спальное место"
    - Use `parse_price()` from utils.
    - All optional fields default to None.
    - Call `self.delay()` after extraction.

    **Document all discovered selectors as comments in the code** for future maintenance.

    **Register in run_scrapers.py:** Add OrmatemScraper to the spider registry.
  </action>
  <verify>
    <automated>cd "/Users/ilyamukha/Desktop/Рабочая/Lineaf/Parser" && uv run python -c "from lineaf.scrapers.ormatek import OrmatemScraper; s = OrmatemScraper(); print(f'site={s.site_name}, url={s.catalog_url}')"</automated>
  </verify>
  <done>OrmatemScraper class exists with selectors discovered via live Camoufox session. Discovery results documented as code comments. Spider registered in run_scrapers.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for Ormatek extraction logic</name>
  <files>tests/test_scrapers_ormatek.py</files>
  <behavior>
    - Ormatek field mapping converts discovered Russian labels to DB column names
    - Price extraction handles Ormatek's price format
    - Missing characteristics return None for optional fields
  </behavior>
  <action>
    Create `tests/test_scrapers_ormatek.py`:

    1. Based on what was discovered in Task 1, write unit tests for the pure extraction functions.

    2. Tests (adapt based on actual site structure found):
       - `test_ormatek_field_mapping` — Russian characteristic labels map to correct DB fields
       - `test_ormatek_price_parsing` — site-specific price format parsed correctly
       - `test_ormatek_missing_fields` — missing characteristics → None

    3. Add integration test stub:
       - `test_ormatek_catalog_urls` — marked `@pytest.mark.integration`, `@pytest.mark.skip`.
  </action>
  <verify>
    <automated>cd "/Users/ilyamukha/Desktop/Рабочая/Lineaf/Parser" && uv run pytest tests/test_scrapers_ormatek.py -x -v -m "not integration"</automated>
  </verify>
  <done>Unit tests for Ormatek extraction logic pass. Field mapping and price parsing verified against discovered site structure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all spiders against live sites</name>
  <files>n/a</files>
  <action>Human verifies end-to-end scraping of all three sites against live PostgreSQL database.</action>
  <what-built>All three spiders (Askona, Sonum, Ormatek) implemented with shared BaseScraper, pipeline, and change detection. This checkpoint verifies end-to-end scraping works against live sites.</what-built>
  <how-to-verify>
    1. Ensure Docker PostgreSQL is running: `docker compose up -d`
    2. Apply migrations: `cd "/Users/ilyamukha/Desktop/Рабочая/Lineaf/Parser" && uv run alembic upgrade head`
    3. Run Askona spider only:
       `cd "/Users/ilyamukha/Desktop/Рабочая/Lineaf/Parser" && uv run python -m lineaf.run_scrapers --sites askona`
       Expected: ~75 products scraped, scrape_run logged as "success"
    4. Run all three spiders:
       `cd "/Users/ilyamukha/Desktop/Рабочая/Lineaf/Parser" && uv run python -m lineaf.run_scrapers`
       Expected: products from all 3 sites in DB, 3 scrape_run entries
    5. Verify data in DB:
       `uv run python -c "from lineaf.database import SessionLocal; from lineaf.models import Product, ScrapeRun; db = SessionLocal(); print(f'Products: {db.query(Product).count()}'); print(f'Runs: {db.query(ScrapeRun).count()}'); [print(f'  {r.site}: {r.status}, {r.products_found} found') for r in db.query(ScrapeRun).all()]"`
    6. Run a second time — verify 0 new, 0 removed (idempotent scrape)
  </how-to-verify>
  <verify>Human confirms all three spiders produce data</verify>
  <done>All three spiders produce products in the database, scrape_runs logged correctly, second run is idempotent.</done>
  <resume-signal>Type "approved" if all 3 spiders produce data, or describe which spider(s) failed and what errors appeared in the logs.</resume-signal>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_scrapers_ormatek.py -x -v -m "not integration"` — Ormatek unit tests pass
2. `uv run python -c "from lineaf.scrapers.ormatek import OrmatemScraper"` — imports cleanly
3. All three spiders registered in run_scrapers.py
4. Human verification of live scraping against all 3 sites
</verification>

<success_criteria>
- OrmatemScraper implemented with selectors discovered via Camoufox
- All three spiders produce products when run against live sites
- Second scrape run produces 0 new / 0 removed (idempotent)
- scrape_runs table logs all runs with correct status and counts
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-scrapers/02-04-SUMMARY.md`
</output>
