---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lineaf/models/product.py
  - src/lineaf/models/price_snapshot.py
  - src/lineaf/models/scrape_run.py
  - src/lineaf/models/__init__.py
  - alembic/env.py
  - alembic/versions/0001_initial_schema.py
  - tests/conftest.py
  - tests/test_models.py
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04

must_haves:
  truths:
    - "All three tables (products, price_snapshots, scrape_runs) exist in the database after running Alembic migrations"
    - "Inserting a product with a duplicate (source_site, source_url) raises IntegrityError"
    - "price_original and price_sale are stored and retrieved as Python Decimal, not float"
    - "is_active defaults to True and can be set to False"
    - "A product + price_snapshot round-trip insert works correctly"
    - "scrape_runs stores status, timestamps, and item counts"
  artifacts:
    - path: "src/lineaf/models/product.py"
      provides: "Product model with deduplication constraint"
      contains: "class Product"
      exports: ["Product"]
    - path: "src/lineaf/models/price_snapshot.py"
      provides: "PriceSnapshot model with NUMERIC(12,2) prices"
      contains: "class PriceSnapshot"
      exports: ["PriceSnapshot"]
    - path: "src/lineaf/models/scrape_run.py"
      provides: "ScrapeRun model for run logging"
      contains: "class ScrapeRun"
      exports: ["ScrapeRun"]
    - path: "alembic/versions/0001_initial_schema.py"
      provides: "Initial migration creating all three tables"
      contains: "op.create_table"
    - path: "tests/conftest.py"
      provides: "Test DB engine and session fixtures with rollback"
      contains: "db_session"
    - path: "tests/test_models.py"
      provides: "Tests for STOR-01 through STOR-04"
      contains: "test_product_deduplication"
  key_links:
    - from: "src/lineaf/models/price_snapshot.py"
      to: "src/lineaf/models/product.py"
      via: "ForeignKey products.id"
      pattern: "ForeignKey.*products\\.id"
    - from: "src/lineaf/models/price_snapshot.py"
      to: "src/lineaf/models/scrape_run.py"
      via: "ForeignKey scrape_runs.id"
      pattern: "ForeignKey.*scrape_runs\\.id"
    - from: "alembic/env.py"
      to: "src/lineaf/models/"
      via: "model imports for autogenerate"
      pattern: "from lineaf.models import"
    - from: "tests/conftest.py"
      to: "src/lineaf/models/base.py"
      via: "Base.metadata.create_all for test DB"
      pattern: "Base.metadata.create_all"
---

<objective>
Create SQLAlchemy 2.0 models for all three tables (products, price_snapshots, scrape_runs), generate the initial Alembic migration, and write tests proving all STOR requirements are met.

Purpose: Deliver the database schema contract that scrapers (Phase 2) write to and the dashboard (Phase 3) reads from. This is the single most important artifact of Phase 1.
Output: Three model files, one Alembic migration, and a passing test suite covering STOR-01 through STOR-04.
</objective>

<execution_context>
@/Users/ilyamukha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ilyamukha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01-01 — executor needs these to build models on top of -->

From src/lineaf/models/base.py:
```python
from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
    pass
```

From src/lineaf/config.py:
```python
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")
    database_url: str
    test_database_url: str = "postgresql://lineaf:lineaf@localhost:5432/lineaf_test"
    debug: bool = False

settings = Settings()
```

From src/lineaf/database.py:
```python
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

engine = create_engine(settings.database_url, echo=settings.debug)
SessionLocal = sessionmaker(bind=engine, autocommit=False, autoflush=False)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SQLAlchemy models for products, price_snapshots, and scrape_runs</name>
  <files>src/lineaf/models/product.py, src/lineaf/models/price_snapshot.py, src/lineaf/models/scrape_run.py, src/lineaf/models/__init__.py</files>
  <behavior>
    - STOR-01: Product with (source_site, source_url) unique constraint; duplicate insert raises IntegrityError
    - STOR-02: PriceSnapshot with price_original and price_sale as NUMERIC(12,2); round-trip preserves Decimal("24999.99") exactly
    - STOR-03: ScrapeRun with status (TEXT), started_at, finished_at, products_found, products_new, products_removed, error_message
    - STOR-04: Product.is_active defaults to True, can be set to False and persisted
    - PriceSnapshot.product_id is FK to products.id
    - PriceSnapshot.scrape_run_id is FK to scrape_runs.id (nullable)
    - Product has relationship to price_snapshots (backref for test convenience)
  </behavior>
  <action>
1. Create `src/lineaf/models/product.py`:
   - Import Base from `lineaf.models.base`
   - Use SQLAlchemy 2.0 style: `Mapped[]` + `mapped_column()`
   - Columns: id (int PK), source_site (String(50) NOT NULL), source_url (String(2048) NOT NULL), name (String(512) NOT NULL)
   - Nullable TEXT attribute columns: firmness (String(256)), height_cm (String(64)), filler (String(512)), cover_material (String(256)), weight_kg (String(64))
   - is_active (Boolean NOT NULL default=True) — per STOR-04
   - first_seen_at (DateTime(timezone=True) server_default=func.now())
   - updated_at (DateTime(timezone=True) server_default=func.now(), onupdate=func.now())
   - UniqueConstraint("source_site", "source_url", name="uq_products_site_url") — per STOR-01
   - Relationship: `price_snapshots` back_populates or lazy relationship to PriceSnapshot

2. Create `src/lineaf/models/price_snapshot.py`:
   - Columns: id (int PK), product_id (FK products.id NOT NULL), scraped_at (DateTime(timezone=True) NOT NULL), scrape_run_id (FK scrape_runs.id, nullable)
   - price_original (NUMERIC(12,2) nullable), price_sale (NUMERIC(12,2) nullable) — per STOR-02, user locked NUMERIC not float
   - Relationship back to Product

3. Create `src/lineaf/models/scrape_run.py`:
   - Columns: id (int PK), site (String(50) NOT NULL), status (String(32) NOT NULL default="running")
   - started_at (DateTime(timezone=True) server_default=func.now()), finished_at (DateTime(timezone=True) nullable)
   - products_found (Optional[int]), products_new (Optional[int]), products_removed (Optional[int])
   - error_message (String(2048) nullable) — per STOR-03

4. Update `src/lineaf/models/__init__.py` to import all three models so they are registered with Base.metadata.

5. Update `alembic/env.py` model imports to include product, price_snapshot, scrape_run (ensure autogenerate detects all tables — see Pitfall 1 in RESEARCH.md).
  </action>
  <verify>
    <automated>python -c "from lineaf.models.product import Product; from lineaf.models.price_snapshot import PriceSnapshot; from lineaf.models.scrape_run import ScrapeRun; from lineaf.models.base import Base; tables = Base.metadata.tables; assert 'products' in tables and 'price_snapshots' in tables and 'scrape_runs' in tables; print(f'Tables: {list(tables.keys())}')"</automated>
  </verify>
  <done>All three model classes import cleanly. Base.metadata contains products, price_snapshots, and scrape_runs tables. Product has UniqueConstraint on (source_site, source_url). PriceSnapshot uses NUMERIC(12,2). Product.is_active exists with default=True.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate Alembic migration and create test suite for STOR requirements</name>
  <files>alembic/versions/0001_initial_schema.py, tests/conftest.py, tests/test_models.py</files>
  <behavior>
    - test_product_deduplication: Insert product, insert duplicate (source_site, source_url) -> IntegrityError (STOR-01)
    - test_price_snapshot_decimal: Insert PriceSnapshot with Decimal("29999.00") and Decimal("24999.99"), retrieve -> values match exactly (STOR-02)
    - test_scrape_run_insert: Insert ScrapeRun with all fields, retrieve -> all fields match (STOR-03)
    - test_is_active_flag: Insert Product (is_active defaults True), update to False, retrieve -> False (STOR-04)
    - test_product_price_snapshot_round_trip: Insert Product + PriceSnapshot, query back via relationship -> works
  </behavior>
  <action>
1. Start PostgreSQL: `docker compose up -d` and wait for healthy.

2. Create the test database if it doesn't exist: run `docker compose exec db psql -U lineaf -d lineaf_dev -c "SELECT 1 FROM pg_database WHERE datname='lineaf_test'" | grep -q 1 || docker compose exec db psql -U lineaf -d lineaf_dev -c "CREATE DATABASE lineaf_test"`.

3. Generate the initial Alembic migration:
   `uv run alembic revision --autogenerate -m "initial schema"`
   Verify the generated migration file contains `op.create_table("products"...)`, `op.create_table("price_snapshots"...)`, `op.create_table("scrape_runs"...)`.
   If autogenerate produces empty upgrade(), models are not imported — fix alembic/env.py imports.

4. Apply migration to dev database: `uv run alembic upgrade head`

5. Create `tests/conftest.py`:
   - Read TEST_DATABASE_URL from environment (with fallback to `postgresql://lineaf:lineaf@localhost:5432/lineaf_test`)
   - Create a test engine with `create_engine(TEST_DATABASE_URL)`
   - Fixture `db_engine`: creates all tables via `Base.metadata.create_all(engine)`, yields engine, drops all via `Base.metadata.drop_all(engine)` on teardown
   - Fixture `db_session`: creates a `Session(bind=engine)`, yields it, rolls back and closes on teardown
   - Import all models so Base.metadata is populated

6. Create `tests/test_models.py` with tests:

   **test_product_deduplication (STOR-01):**
   - Insert a Product(source_site="askona", source_url="https://askona.ru/p/1", name="M1")
   - Flush, then insert another Product with same source_site+source_url
   - Assert `IntegrityError` is raised on flush

   **test_price_snapshot_decimal (STOR-02):**
   - Insert Product, flush to get id
   - Insert PriceSnapshot with price_original=Decimal("29999.00"), price_sale=Decimal("24999.99"), scraped_at=now
   - Commit, re-query the snapshot
   - Assert retrieved price_original == Decimal("29999.00") and price_sale == Decimal("24999.99")
   - Assert type is Decimal, not float

   **test_scrape_run_insert (STOR-03):**
   - Insert ScrapeRun(site="askona", status="success", products_found=42, products_new=3, products_removed=1)
   - Commit, re-query
   - Assert all fields match

   **test_is_active_flag (STOR-04):**
   - Insert Product with is_active not set -> assert is_active is True (default)
   - Update is_active to False, commit, re-query -> assert is_active is False

   **test_product_price_snapshot_round_trip:**
   - Insert Product, flush, insert PriceSnapshot with product_id
   - Commit, query Product, access price_snapshots relationship
   - Assert relationship returns the snapshot with correct prices

7. Run the full test suite: `uv run pytest tests/ -x -v`
  </action>
  <verify>
    <automated>docker compose up -d && sleep 3 && uv run alembic upgrade head && uv run pytest tests/test_models.py -x -v</automated>
  </verify>
  <done>Alembic migration creates all three tables. All 5 tests pass: deduplication constraint works (STOR-01), prices are Decimal not float (STOR-02), scrape_runs stores all fields (STOR-03), is_active defaults True and can be set False (STOR-04), round-trip product+snapshot works.</done>
</task>

</tasks>

<verification>
1. `docker compose up -d` starts PostgreSQL and it becomes healthy
2. `uv run alembic upgrade head` applies migration without errors
3. `uv run pytest tests/ -x -v` — all 5 tests green
4. `uv run alembic downgrade base && uv run alembic upgrade head` — migration is reversible
5. Tables verified: `docker compose exec db psql -U lineaf -d lineaf_dev -c "\dt"` shows products, price_snapshots, scrape_runs
</verification>

<success_criteria>
1. Three SQLAlchemy 2.0 model files exist with correct column types
2. Alembic initial migration creates all tables with correct schema
3. Product deduplication constraint on (source_site, source_url) proven by test
4. Price columns use NUMERIC(12,2) and round-trip as Decimal, proven by test
5. is_active defaults True and can toggle to False, proven by test
6. ScrapeRun stores all operational fields, proven by test
7. Full test suite passes: `uv run pytest tests/ -x -v` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
